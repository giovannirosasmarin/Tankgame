package window;

import javax.swing.*;
import java.awt.*;

//not needed any more because the window was implemented directly in  init()
public class Window
{
    public Window(int w, int h, String title, tankGame game)
    {
//        game.setPreferredSize ( new Dimension ( w,h ) );
//        game.setMaximumSize  ( new Dimension ( w,h ) );
//        game.setMinimumSize  ( new Dimension ( w,h ) );
//
//        JFrame frame = new JFrame ( title ); //Title in the top of the window
//        frame.add(game);//take the dimension
//        frame.pack ();                            //JFrame.EXIT_ON_CLOSE
//        frame.setDefaultCloseOperation ( WindowConstants.EXIT_ON_CLOSE );  // it closes when click "x" so it does not run in the background
//        frame.setResizable ( false );  // change to true if want to make full screen
//        frame.setLocationRelativeTo ( null );  // opens in the center of the screen
//        frame.setVisible ( true );  // it visible when started
//
//        game.start ();

    }

}
